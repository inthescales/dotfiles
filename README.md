# .dotfiles

These are my personal files for configuring the terminal.

## Setup Instructions

1. Clone this repo into `~/.dotfiles`
2. Set the `.zprofile` and `.zshrc` files in `~/` to source the matching files in `.dotfiles/`
